name: CI Pipeline
on: [push]
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker Image
        run: docker build -t shadoww002/fakenews-api:latest -f deployment/Dockerfile .
      # Add Docker Hub push logic here