# --- Frontend (UI & Dashboard) ---
streamlit>=1.30.0
requests>=2.31.0

# --- Backend (API & Routing) ---
fastapi>=0.109.0
uvicorn>=0.27.0

# --- Reasoning Engine (Local LLM) ---
ollama>=0.1.6

# --- RAG Core (Retrieval & Embedding) ---
gnews>=0.3.6
sentence-transformers>=2.3.0
faiss-cpu>=1.7.4

# --- Note on Hardware ---
# faiss-cpu is used here for maximum compatibility across all machines. 
# If you are deploying this on a machine with a dedicated Nvidia GPU, 
# you can swap 'faiss-cpu' for 'faiss-gpu' for faster vector searches.
